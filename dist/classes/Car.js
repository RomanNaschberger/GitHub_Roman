export default class Car {
    constructor(brand, model, engine) {
        this._model = '';
        this._brand = brand;
        this._model = model;
        this._engine = engine;
    }
}
console.log('test');
//# sourceMappingURL=Car.js.map